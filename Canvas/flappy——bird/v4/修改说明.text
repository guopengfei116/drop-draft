修改说明：
1. 文件进行了目录分类
2. 在flappyBird主游戏中，调用场景的draw方法时，统一传入一个绘制上下文。
3. roles目录中的代码做了调整，同时小鸟和管道使用了加速度，其他均是匀速

新增修改：
1. 增加了一个存储base64背景图的js文件，用作loadding场景使用
2. 增加了button类
3. 游戏loadding场景和over场景增加了按钮
4. 去掉了roles里面的工厂，方便游戏重新开始时数据的初始化工作